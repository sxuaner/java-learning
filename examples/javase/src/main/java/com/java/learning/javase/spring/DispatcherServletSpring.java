package com.java.learning.javase.spring;

import org.springframework.web.servlet.DispatcherServlet;

/**
 * The DispatcherServlet is an actual Servlet (it inherits from the HttpServlet
 * base class), and as such is declared in the web.xml of your web application.
 * You need to map requests that you want the DispatcherServlet to handle, by
 * using a URL mapping in the same web.xml file.
 */

public class DispatcherServletSpring {
    DispatcherServlet ds;

    public static void main(String[] args) {
    }
    
}
