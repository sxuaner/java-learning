package com.java.learning.javase;

/**
 * @FunctionalInterface public interface UnaryOperator<T> extends Function<T,​T>
 * 
 *                      Represents an operation on a single operand that
 *                      produces a result of the same type as its operand. This
 *                      is a specialization of Function for the case where the
 *                      operand and result are of the same type.
 * 
 */
public class UnaryOperatorSE {
    /**
     * # Unary: (especially of a mathematical operation) consisting of or involving
     *          a single component or element.
     */
     public static void main(String[] args) {
         
     }
    
}
